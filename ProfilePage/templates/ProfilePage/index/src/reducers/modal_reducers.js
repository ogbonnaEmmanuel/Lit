const initState = {
    form_modal_state: {
        loading: false,
        link_created: false,
    }
}

const modalReducer = (state=initState,action)=>{
    switch (action.type) {

    }
}
export default modalReducer;